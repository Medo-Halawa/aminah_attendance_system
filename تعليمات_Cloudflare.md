# تعليمات نقل نظام الحضور لـ Cloudflare (D1 + Pages Functions)

## الملفات اللي معاك
- **schema.sql** → أوامر إنشاء الجداول في قاعدة البيانات
- **functions/** (مجلد كامل: `_utils.js` + `api/employees.js` + `api/today.js` + `api/checkin.js` + `api/checkout.js`) → الباك إند
- **attendance_web.html** → الواجهة (نفس التصميم، بس بيتكلم مع D1 بدل جوجل شيت)

## الخطوات

### 1) اعمل قاعدة بيانات D1
1. من لوحة تحكم Cloudflare: **Workers & Pages ← D1 SQL Database ← Create Database**.
2. سمّيها مثلاً `attendance_db`.
3. افتح تاب **Console** جوا قاعدة البيانات، افتح ملف `schema.sql` اللي بعتهولك، انسخ محتواه كله، والصقه في الـ Console، واضغط **Execute**.
4. هيتعملك جدولين: `employees` و `attendance`.

### 2) ضيف الموظفين
- من نفس تاب الـ Console، اكتب أوامر INSERT لكل موظف، مثلاً:
  ```sql
  INSERT INTO employees (employee_name, job_title) VALUES ('سارة أحمد', 'ريسيبشن');
  INSERT INTO employees (employee_name, job_title) VALUES ('محمد سامي', 'تمريض داخلي');
  ```
- كرر السطر ده لكل موظف عندك، وغيّر الاسم والوظيفة.

### 3) رتّب ملفات المشروع
لازم شكل مجلدات مشروعك يبقى كده بالظبط:
```
اسم-مشروعك/
├── attendance_web.html   (سمّيه index.html عشان يفتح تلقائي)
└── functions/
    ├── _utils.js
    └── api/
        ├── employees.js
        ├── today.js
        ├── checkin.js
        └── checkout.js
```
⚠️ **مهم:** سمّي ملف `attendance_web.html` إلى **`index.html`** عشان يفتح تلقائي لما حد يدخل على رابط الموقع من غير ما يكتب اسم الملف.

### 4) اربط قاعدة البيانات بمشروع الـ Pages
1. من مشروع الـ Pages بتاعك في Cloudflare ← **Settings ← Functions ← D1 database bindings**.
2. اضغط **Add binding**.
3. **Variable name:** اكتب `DB` بالظبط (الكود بيستخدم `env.DB`).
4. **D1 database:** اختار `attendance_db` اللي عملته.
5. احفظ.

### 5) ارفع الملفات
- لو بتنشر عن طريق **ربط GitHub**: ادفع (push) الملفات دي لنفس الريبو، وCloudflare هتعمل نشر تلقائي.
- لو بتنشر عن طريق **رفع مباشر (Direct Upload)**: من مشروع الـ Pages اضغط **Create deployment** وارفع مجلد المشروع كله (لازم يشمل مجلد `functions` جواه).

### 6) جرب
- افتح رابط موقعك، هتلاقي أسماء الموظفين بتظهرلك وأنت بتكتب، ولو الاسم مطابق لموظف مسجل، الوظيفة هتتملى لوحدها.
- سجل حضور تجريبي، وتأكد إنه ظهر في جدول `attendance` من الـ D1 Console:
  ```sql
  SELECT * FROM attendance ORDER BY id DESC LIMIT 5;
  ```

## قواعد الشيفت (تقدر تغيّرها)
- كل الوظائف **إلا التمريض**: الشيفت بيتحدد أوتوماتيك حسب وقت التسجيل:
  - **AB**: من 8:00 صباحًا لحد 8:00 مساءً
  - **C**: من 8:00 مساءً لحد 8:00 صباحًا
- وظائف **التمريض الثلاثة** (داخلي / عمليات / حضانه): بيظهرلهم اختيار يدوي:
  - **AB** (8 صباحًا - 8 مساءً)
  - **C** (8 مساءً - 8 صباحًا)
  - **ABC** (24 ساعة كاملة)
- لو عايز تغيّر مواعيد الشيفتات، عدّل دالة `computeAutoShift()` في ملف `functions/_utils.js`.

## التعامل مع البقاء أكتر من يوم (شيفت الـ24 ساعة)
النظام بيراعي إن الموظف (خصوصًا صاحب شيفت ABC) ممكن يسجل حضور يوم ويسجل انصرافه اليوم اللي بعده:
- البحث عن "هل الموظف لسه مسجل حضور ولسه ما انصرفش" **بيدور في كل التاريخ، مش بس اليوم الحالي**. يعني لو حد فاضل مسجل حضور من إمبارح، النظام هيلاقيه ويقفله صح لما يسجل انصراف، أيًا كان تاريخ اليوم الحالي.
- بنفس المنطق، مينفعش الموظف يسجل حضور جديد وهو أصلاً لسه "مفتوح" من قبل كده (يعني لازم يقفل الحضور القديم بانصراف الأول قبل ما يبدأ حضور جديد)، بغض النظر عن التاريخ.
- ملحوظة: صف الحضور بيفضل مسجل بتاريخ **يوم الحضور نفسه** (مش يوم الانصراف)، فلو بتراجع "سجل اليوم" هتلاقي العملية دي ظاهرة في يوم الحضور، حتى لو الانصراف الفعلي حصل في اليوم اللي بعده.

## ملاحظات
- ✅ نفس منطق الحماية بموقع GPS شغال زي الأول (250 متر حول نفس إحداثيات المركز).
- ✅ إضافة/تعديل موظف بتتم من D1 Console مباشرة (INSERT / UPDATE / DELETE على جدول employees) زي ما اتفقنا.
- عمود "عدد الساعات" مش متخزن في قاعدة البيانات (مش موجود في التصميم اللي طلبته)، لكن بيتحسب ويتعرض في "سجل اليوم" تلقائي من فرق وقت الحضور والانصراف.
- لو عايز تشوف بيانات شهر كامل بدل اليوم بس، تقدر تعمل استعلام SQL من الـ Console زي:
  ```sql
  SELECT * FROM attendance WHERE date LIKE '%/08/2026';
  ```

## لو حصلت مشكلة
- **"تعذر الاتصال بقاعدة البيانات"** → تأكد إن الـ binding اسمه `DB` بالظبط في إعدادات المشروع.
- أسماء الموظفين مش بتظهر → تأكد إنك نفذت أوامر INSERT في جدول employees فعلاً.
- الموقع بيرجع 404 → تأكد إن ملف الـ HTML اسمه `index.html` وإن مجلد `functions` في نفس مستوى الملف مش جواه.
